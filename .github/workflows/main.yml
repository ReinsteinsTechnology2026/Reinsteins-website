name: Deploy Reinsteins Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Update repository
        run: |
          cd /home/reinsteins/Reinsteins-website
          git fetch origin main
          git reset --hard origin/main

      - name: Restart FastAPI
        run: |
          sudo -n /usr/bin/systemctl restart reinsteins-backend
          sudo -n /usr/bin/systemctl is-active reinsteins-backend

      - name: Reload Nginx
        run: |
          sudo -n /usr/sbin/nginx -t
          sudo -n /usr/bin/systemctl reload nginx

      - name: Deployment complete
        run: |
          echo "Reinsteins website deployed successfully."
